#!/usr/bin/env bash
#-------------------------------------------------------
# Written by Erik Poupaert
# Phnom Penh, Cambodia, March 2013
# Licensed under the General Public License (GPL)
#-------------------------------------------------------

function htpasswd_user_validate
{
        htpasswdUser="$1"
	if [[ "$htpasswdUser" = "" ]] ; then
                echo "ERROR: invalid user name." >&2
                exit 1
	fi
}

